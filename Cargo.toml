[package]
name = "plato-prompt-builder"
version = "0.1.0"
edition = "2021"
description = "Compose LLM prompts from tile search results — PLATO fleet component"
license = "MIT"

[dependencies]
# zero external dependencies
